import type { Validator } from '../types';

/**
 * This file previously contained a static list of Chiliz Chain validators as a fallback.
 * It has been cleared to ensure the app relies solely on live API data.
 */
export const VALIDATORS: Validator[] = [];